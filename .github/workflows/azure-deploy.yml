name: Deploy to Azure

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  NODE_VERSION: '22.x'
  AZURE_WEBAPP_NAME: trimeter-app

jobs:
  build-and-test:
    name: Testing
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Node.js ${{ env.NODE_VERSION }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Build application
      run: npm run build --if-present

    - name: Upload build artifact
      uses: actions/upload-artifact@v4
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      with:
        name: node-app
        path: .
        retention-days: 1

  deploy:
    name: Deploying to Azure
    runs-on: ubuntu-latest
    needs: build-and-test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    environment:
      name: production
      url: https://trimeter-app-c9asesetbbgkbmgc.southeastasia-01.azurewebsites.net/

    steps:
    - name: Download build artifact
      uses: actions/download-artifact@v4
      with:
        name: node-app

    - name: Azure Login
      uses: azure/login@v2
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Deploy to Azure Web App
      uses: azure/webapps-deploy@v3
      with:
        app-name: ${{ env.AZURE_WEBAPP_NAME }}
        package: .

    - name: Azure Logout
      run: az logout
      if: always()

    - name: Deployment complete
      run: |
        echo "Deployment successful!"
        echo "App URL: https://trimeter-app-c9asesetbbgkbmgc.southeastasia-01.azurewebsites.net/"